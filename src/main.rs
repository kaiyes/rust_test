fn main() {
    //booleans
    let _is_correct: bool = true;

    //characters
    let _one_char: char = 'a'; //single quotes not double

    //define multiple variables on multiple lines
    let (country, capital) = ("Malaysia", "KL");
    println!("{}: {}", country, capital);
}
